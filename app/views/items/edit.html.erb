<h2> <%= @list.month %> - Editing List Items</h2>

<%= form_for [@list, @item] do |form| %>
	<%=  render partial: form%>
<% end %>

<%= link_to 'Back', list_items_path %>
<%= link_to 'Checklist', lists_path %>