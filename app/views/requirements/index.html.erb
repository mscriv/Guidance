<h1>Listing requirements</h1>

<table>
  <thead>
    <tr>
      <th>Year</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @requirements.each do |requirement| %>
      <tr>
        <td><%= requirement.year %></td>
        <td><%= requirement.description %></td>
        <td><%= link_to 'View Diplomas', requirement_diplomas_path(requirement) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Requirement', new_requirement_path %>
