<h1>Listing subjects</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Requisite</th>
      <th>Diploma</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @diploma.subjects.each do |subject| %>
      <tr>
        <td><%= subject.name %></td>
        <td><%= subject.requisite %></td>
        <td><%= subject.diploma.name %></td>

      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Subject', new_diploma_subject_path %>
